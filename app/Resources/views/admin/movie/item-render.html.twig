{% for item in data %}
	<tr class="data-item" data-id="{{ item.id }}">
		<td class="data-item-image">
			<a href="{{ path('catalogue_index',{'slug':item.slug}) }}" target="_blank" data-title="afficher sur le site" data-toggle="tooltip">
				{% if item.portraitImg %}
					<img  src="{{ asset('upload/public/'~item.portraitImg) }}" alt="" />
				{% else %}
					<img  src="{{ asset('admin/dist/img/default-50x50.gif') }}">
				{% endif %}
			</a>
		</td>

		<td class="data-item-name" style="vertical-align:middle;">
			<a href="" class="edit">
				{{ item.name|lower|title }}
			</a>
			
		</td>

		<td class="data-item-originalname" style="vertical-align:middle;text-align:center">
			{{ item.originalName|lower|title }}
		</td>

		<td class="data-item-category" style="vertical-align:middle;text-align:center" width="100">
			{% if item.category %}
				{{ item.category.name|lower|title }}
			{% else %}
				-
			{% endif %}
		</td>

		<td class="data-item-originalLanguage" style="vertical-align:middle;text-align:center" width="100">
			{% if item.originalLanguage %}
				{{ item.originalLanguage|lower|title }}
			{% else %}
				-
			{% endif %}
		</td>

		<td class="data-item-format" style="vertical-align:middle;text-align:center" width="100">
			{% if item.format %}
				{{ item.format|lower|title }}
			{% else %}
				-
			{% endif %}
		</td>

		<td class="data-item-mention" style="vertical-align:middle;text-align:center" width="100">
			{% if item.mention %}
				{{ item.mention|lower|title }}
			{% else %}
				-
			{% endif %}
		</td>

		<td class="data-item-year" style="vertical-align:middle;text-align:center" width="100">
			{% if item.yearEnd %}
				{% if item.yearEnd == item.yearStart %}
					<span class="label label-default">
						{{ item.yearStart|date("d/m/Y") }}
					</span>
				{% else %}
					<span class="label label-default">
						{{ item.yearStart|date("d-m-Y") }}
					</span>
					
					<div>
						<i class="fa fa-arrow-down text-muted"></i>
					</div>

					<span class="label label-default">
						{{ item.yearEnd|date("d-m-Y") }}
					</span> 
				{% endif %}
				
			{% else %}
				<span class="label label-default">
					{{ item.yearStart|date("d/m/Y") }}
				</span>
			{% endif %}
		</td>

		<td class="data-item-theather" style="vertical-align:middle;text-align:center" width="100">
			{% if item.inTheather %}
				<span class="label bg-green">
					Oui
				</span> 
			{% else %}
				<span class="label label-default">
					Non
				</span> 
			{% endif %}
		</td>

		<td class="data-item-exclusivity" style="vertical-align:middle;text-align:center" width="100">
			{% if item.hasExclusivity %}
				<span class="label bg-green">
					Oui
				</span> 
			{% else %}
				<span class="label label-default">
					Non
				</span> 
			{% endif %}
		</td>

		<td class="data-item-exclusivity" style="vertical-align:middle;text-align:center" width="100">
			{% if item.isPublished %}
				<span class="label bg-green">
					Publié
				</span> 
			{% else %}
				<span class="label label-default">
					Pre-modération
				</span> 
			{% endif %}
		</td>

		<td class="data-item-movies" style="vertical-align:middle;text-align:center">
			
			{% if item.trailer %}
				<a data-vbtype="video" numeratio="false" data-autoplay="true" href="{{ item.trailer }}" class="venobox" >Trailer</a> 
			{% endif %}

			{% if item.episode1 %}
				. <a data-vbtype="video" numeratio="false" data-autoplay="true" href="{{ item.episode1 }}" class="venobox" >Episode 1</a> 
			{% endif %}

			{% if item.episode2 %}
				. <a data-vbtype="video" numeratio="false" data-autoplay="true" href="{{ item.episode2 }}" class="venobox" >Episode 2</a> 
			{% endif %}

			{% if item.episode3 %}
				<a data-vbtype="video" numeratio="false" data-autoplay="true" href="{{ item.episode3 }}" class="venobox" >Episode 3</a> 
			{% endif %}

			{% if not item.episode1 and not item.episode2 and not item.episode3  %}
				-
			{% endif %}

		</td>

		{#<td style="vertical-align:middle">
			{{ item.createAt|date("d/m/Y à H:i") }}
		</td>#}
	</tr>
{% else %}
	{% if use_modal is defined %}
		<tr>
			<td colspan="12">
				<div class="alert alert-dismissible">
		        	<button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
		        	<h4><i class="icon fa fa-warning"></i> Oops!</h4>
		        	Aucun resultat ne correspont à votre recherche.
		        	<br>
		        	Veuillez réessayer
		      	</div>
			</td>
		</tr>
		
	{% endif %}
{% endfor %}