{% form_theme form 'bootstrap_3_layout.html.twig' %}


<div class="box box-primary" id="current-widget-data" data-id="{{ data.id }}">
    <div class="box-header with-border">
    	<h3 class="box-title">Modification</h3>
    </div>

    <div class="box-body">
    	{{ form(form) }}

	    <div  style="text-align:center;position:relative">
	    	{% if data.image %}
				<img  width="270" height="360" id="thumbnail-trailer" src="{{ asset('upload/public/'~data.image) }}" alt="" />
			{% else %}
	    		<img width="270" height="360" id="thumbnail-trailer"  src="{{ asset('/img/vignette.png') }}">
			{% endif %}

			<div class="prs_upcom_video_img_cont_over">
				<h2>Image à charger</h2>
				<button class="btn btn-info btn-block">Annuler</button>
			</div>
		</div>
    </div>

    <div class="box-footer">
    	<button type="reset" class="btn btn-default" >
  			Annuler
  		</button>

    	<button type="submit" class="btn btn-danger pull-right">
    		<i class="fa fa-save"></i> Enregistrer
    	</button>
    </div>
</div>